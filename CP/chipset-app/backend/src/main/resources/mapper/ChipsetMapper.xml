<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chipset.mapper.ChipsetMapper">

  <!-- ══ 메인: DELETE (파일 타입별) ══════════════════════════════════ -->

  <delete id="deleteCellsByFileType">
    DELETE FROM CHIPSET_CELL
    WHERE UPLOAD_SEQ IN (
      SELECT UPLOAD_SEQ FROM CHIPSET_UPLOAD WHERE FILE_TYPE = #{fileType}
    )
  </delete>

  <delete id="deleteChipColsByFileType">
    DELETE FROM CHIPSET_CHIP_COL
    WHERE UPLOAD_SEQ IN (
      SELECT UPLOAD_SEQ FROM CHIPSET_UPLOAD WHERE FILE_TYPE = #{fileType}
    )
  </delete>

  <delete id="deleteSpecColsByFileType">
    DELETE FROM CHIPSET_SPEC_COL
    WHERE UPLOAD_SEQ IN (
      SELECT UPLOAD_SEQ FROM CHIPSET_UPLOAD WHERE FILE_TYPE = #{fileType}
    )
  </delete>

  <delete id="deleteRawdataColsByFileType">
    DELETE FROM CHIPSET_RAWDATA_COL
    WHERE UPLOAD_SEQ IN (
      SELECT UPLOAD_SEQ FROM CHIPSET_UPLOAD WHERE FILE_TYPE = #{fileType}
    )
  </delete>

  <delete id="deleteUploadsByFileType">
    DELETE FROM CHIPSET_UPLOAD WHERE FILE_TYPE = #{fileType}
  </delete>

  <!-- ══ 메인: INSERT ════════════════════════════════════════════════ -->

  <insert id="insertUpload" parameterType="com.chipset.model.ChipsetUpload">
    <selectKey keyProperty="uploadSeq" resultType="long" order="BEFORE">
      SELECT SQ_CHIPSET_UPLOAD.NEXTVAL FROM DUAL
    </selectKey>
    INSERT INTO CHIPSET_UPLOAD (UPLOAD_SEQ, FILE_NM, FILE_TYPE, UPLOAD_DT, ROW_COUNT, COL_COUNT)
    VALUES (#{uploadSeq}, #{fileNm}, #{fileType}, CURRENT_TIMESTAMP, #{rowCount}, #{colCount})
  </insert>

  <insert id="insertChipCol" parameterType="com.chipset.model.ChipsetChipCol">
    <selectKey keyProperty="colSeq" resultType="long" order="BEFORE">
      SELECT SQ_CHIPSET_CHIP_COL.NEXTVAL FROM DUAL
    </selectKey>
    INSERT INTO CHIPSET_CHIP_COL
      (COL_SEQ, UPLOAD_SEQ, VENDOR, COL_IDX, CHIP_NM, CHIP_DT, SORT_ORDER)
    VALUES
      (#{colSeq}, #{uploadSeq}, #{vendor}, #{colIdx}, #{chipNm}, #{chipDt}, #{sortOrder})
  </insert>

  <insert id="insertSpecCol" parameterType="com.chipset.model.ChipsetSpecCol">
    <selectKey keyProperty="colSeq" resultType="long" order="BEFORE">
      SELECT SQ_CHIPSET_SPEC_COL.NEXTVAL FROM DUAL
    </selectKey>
    INSERT INTO CHIPSET_SPEC_COL (COL_SEQ, UPLOAD_SEQ, COL_IDX, COL_NM, SORT_ORDER)
    VALUES (#{colSeq}, #{uploadSeq}, #{colIdx}, #{colNm}, #{sortOrder})
  </insert>

  <insert id="insertRawdataCol" parameterType="com.chipset.model.ChipsetRawdataCol">
    <selectKey keyProperty="colSeq" resultType="long" order="BEFORE">
      SELECT SQ_CHIPSET_RAWDATA_COL.NEXTVAL FROM DUAL
    </selectKey>
    INSERT INTO CHIPSET_RAWDATA_COL (COL_SEQ, UPLOAD_SEQ, COL_IDX, COL_NM, SORT_ORDER)
    VALUES (#{colSeq}, #{uploadSeq}, #{colIdx}, #{colNm}, #{sortOrder})
  </insert>

  <insert id="insertCell" parameterType="com.chipset.model.ChipsetCell">
    <selectKey keyProperty="cellSeq" resultType="long" order="BEFORE">
      SELECT SQ_CHIPSET_CELL.NEXTVAL FROM DUAL
    </selectKey>
    INSERT INTO CHIPSET_CELL
      (CELL_SEQ, UPLOAD_SEQ, ROW_IDX, COL_TYPE, COL_SEQ, CELL_VALUE, BG_COLOR)
    VALUES
      (#{cellSeq}, #{uploadSeq}, #{rowIdx}, #{colType}, #{colSeq}, #{cellValue}, #{bgColor})
  </insert>

  <!-- ══ 메인: SELECT ════════════════════════════════════════════════ -->

  <select id="selectLatestUploadByType" resultType="com.chipset.model.ChipsetUpload">
    SELECT UPLOAD_SEQ, FILE_NM, FILE_TYPE, UPLOAD_DT, ROW_COUNT, COL_COUNT
    FROM CHIPSET_UPLOAD
    WHERE FILE_TYPE = #{fileType}
    ORDER BY UPLOAD_DT DESC
    FETCH FIRST 1 ROWS ONLY
  </select>

  <select id="selectChipColsByType" resultType="com.chipset.model.ChipsetChipCol">
    SELECT c.COL_SEQ, c.UPLOAD_SEQ, c.VENDOR, c.COL_IDX, c.CHIP_NM, c.CHIP_DT, c.SORT_ORDER
    FROM CHIPSET_CHIP_COL c
    WHERE c.UPLOAD_SEQ = (
      SELECT UPLOAD_SEQ FROM CHIPSET_UPLOAD
      WHERE FILE_TYPE = #{fileType}
      ORDER BY UPLOAD_DT DESC
      FETCH FIRST 1 ROWS ONLY
    )
    ORDER BY c.SORT_ORDER
  </select>

  <select id="selectSpecColsByType" resultType="com.chipset.model.ChipsetSpecCol">
    SELECT s.COL_SEQ, s.UPLOAD_SEQ, s.COL_IDX, s.COL_NM, s.SORT_ORDER
    FROM CHIPSET_SPEC_COL s
    WHERE s.UPLOAD_SEQ = (
      SELECT UPLOAD_SEQ FROM CHIPSET_UPLOAD
      WHERE FILE_TYPE = #{fileType}
      ORDER BY UPLOAD_DT DESC
      FETCH FIRST 1 ROWS ONLY
    )
    ORDER BY s.SORT_ORDER
  </select>

  <select id="selectRawdataColsByType" resultType="com.chipset.model.ChipsetRawdataCol">
    SELECT r.COL_SEQ, r.UPLOAD_SEQ, r.COL_IDX, r.COL_NM, r.SORT_ORDER
    FROM CHIPSET_RAWDATA_COL r
    WHERE r.UPLOAD_SEQ = (
      SELECT UPLOAD_SEQ FROM CHIPSET_UPLOAD
      WHERE FILE_TYPE = #{fileType}
      ORDER BY UPLOAD_DT DESC
      FETCH FIRST 1 ROWS ONLY
    )
    ORDER BY r.SORT_ORDER
  </select>

  <select id="selectCellsByUploadSeq" resultType="com.chipset.model.ChipsetCell">
    SELECT CELL_SEQ, UPLOAD_SEQ, ROW_IDX, COL_TYPE, COL_SEQ, CELL_VALUE, BG_COLOR
    FROM CHIPSET_CELL
    WHERE UPLOAD_SEQ = #{uploadSeq}
    ORDER BY ROW_IDX, COL_TYPE, COL_SEQ
  </select>

  <!-- ══ 히스토리: INSERT ════════════════════════════════════════════ -->

  <insert id="insertUploadH" parameterType="com.chipset.model.ChipsetUpload">
    INSERT INTO CHIPSET_UPLOAD_H
      (UPLOAD_H_SEQ, UPLOAD_SEQ, FILE_NM, FILE_TYPE, UPLOAD_DT, ROW_COUNT, COL_COUNT)
    VALUES
      (SQ_CHIPSET_UPLOAD_H.NEXTVAL, #{uploadSeq}, #{fileNm}, #{fileType},
       CURRENT_TIMESTAMP, #{rowCount}, #{colCount})
  </insert>

  <insert id="insertChipColH" parameterType="com.chipset.model.ChipsetChipCol">
    INSERT INTO CHIPSET_CHIP_COL_H
      (CHIP_COL_H_SEQ, COL_SEQ, UPLOAD_SEQ, VENDOR, COL_IDX, CHIP_NM, CHIP_DT, SORT_ORDER)
    VALUES
      (SQ_CHIPSET_CHIP_COL_H.NEXTVAL, #{colSeq}, #{uploadSeq},
       #{vendor}, #{colIdx}, #{chipNm}, #{chipDt}, #{sortOrder})
  </insert>

  <insert id="insertSpecColH" parameterType="com.chipset.model.ChipsetSpecCol">
    INSERT INTO CHIPSET_SPEC_COL_H
      (SPEC_COL_H_SEQ, COL_SEQ, UPLOAD_SEQ, COL_IDX, COL_NM, SORT_ORDER)
    VALUES
      (SQ_CHIPSET_SPEC_COL_H.NEXTVAL, #{colSeq}, #{uploadSeq},
       #{colIdx}, #{colNm}, #{sortOrder})
  </insert>

  <insert id="insertRawdataColH" parameterType="com.chipset.model.ChipsetRawdataCol">
    INSERT INTO CHIPSET_RAWDATA_COL_H
      (RAWDATA_COL_H_SEQ, COL_SEQ, UPLOAD_SEQ, COL_IDX, COL_NM, SORT_ORDER)
    VALUES
      (SQ_CHIPSET_RAWDATA_COL_H.NEXTVAL, #{colSeq}, #{uploadSeq},
       #{colIdx}, #{colNm}, #{sortOrder})
  </insert>

  <insert id="insertCellH" parameterType="com.chipset.model.ChipsetCell">
    INSERT INTO CHIPSET_CELL_H
      (CELL_H_SEQ, CELL_SEQ, UPLOAD_SEQ, ROW_IDX, COL_TYPE, COL_SEQ, CELL_VALUE, BG_COLOR)
    VALUES
      (SQ_CHIPSET_CELL_H.NEXTVAL, #{cellSeq}, #{uploadSeq},
       #{rowIdx}, #{colType}, #{colSeq}, #{cellValue}, #{bgColor})
  </insert>

  <!-- ══ 히스토리: SELECT ════════════════════════════════════════════ -->

  <select id="selectHistoryByType" resultType="com.chipset.model.ChipsetUpload">
    SELECT UPLOAD_SEQ, FILE_NM, FILE_TYPE, UPLOAD_DT, ROW_COUNT, COL_COUNT
    FROM CHIPSET_UPLOAD_H
    WHERE FILE_TYPE = #{fileType}
    ORDER BY UPLOAD_DT DESC
  </select>

  <select id="selectChipColsH" resultType="com.chipset.model.ChipsetChipCol">
    SELECT COL_SEQ, UPLOAD_SEQ, VENDOR, COL_IDX, CHIP_NM, CHIP_DT, SORT_ORDER
    FROM CHIPSET_CHIP_COL_H
    WHERE UPLOAD_SEQ = #{uploadSeq}
    ORDER BY SORT_ORDER
  </select>

  <select id="selectSpecColsH" resultType="com.chipset.model.ChipsetSpecCol">
    SELECT COL_SEQ, UPLOAD_SEQ, COL_IDX, COL_NM, SORT_ORDER
    FROM CHIPSET_SPEC_COL_H
    WHERE UPLOAD_SEQ = #{uploadSeq}
    ORDER BY SORT_ORDER
  </select>

  <select id="selectRawdataColsH" resultType="com.chipset.model.ChipsetRawdataCol">
    SELECT COL_SEQ, UPLOAD_SEQ, COL_IDX, COL_NM, SORT_ORDER
    FROM CHIPSET_RAWDATA_COL_H
    WHERE UPLOAD_SEQ = #{uploadSeq}
    ORDER BY SORT_ORDER
  </select>

  <select id="selectCellsHByUploadSeq" resultType="com.chipset.model.ChipsetCell">
    SELECT CELL_SEQ, UPLOAD_SEQ, ROW_IDX, COL_TYPE, COL_SEQ, CELL_VALUE, BG_COLOR
    FROM CHIPSET_CELL_H
    WHERE UPLOAD_SEQ = #{uploadSeq}
    ORDER BY ROW_IDX, COL_TYPE, COL_SEQ
  </select>

</mapper>
